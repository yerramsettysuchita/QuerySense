[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.SAWarning
markers =
    integration: marks tests as requiring live DB (deselect with -m "not integration")
    unit: marks tests as pure unit tests
